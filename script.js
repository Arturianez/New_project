const array = ["one", 'two', 'three', 1,2,3]

for (let i = 0; i < array.length; i++) {
    if(array[i]===String(array[i])){
        console.log(array[i])
    } else {
        console.log(array[i])
    }
}

const a = true;
if(!a){
    console.log('false')
}